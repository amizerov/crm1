# Секреты проекта - Документация

## Обзор

Компонент **Secrets** обеспечивает безопасное хранение и управление конфиденциальными данными проекта (API ключи, пароли БД, токены и т.д.).

## Основные возможности

### 🔐 Безопасность

1. **Мастер-пароль**
   - Доступ к секретам защищен мастер-паролем
   - Пароль проверяется на сервере перед предоставлением доступа
   - Хеш пароля хранится в поле `master_password_hash` таблицы `Projects`

2. **Автоматическая блокировка**
   - Автоматическая блокировка через 5 минут бездействия
   - Отслеживает активность пользователя (клики, нажатия клавиш, скролл)
   - При блокировке все открытые секреты скрываются

3. **Логирование доступа**
   - Все действия с секретами логируются в таблицу `SecretAccessLog`
   - Записывается: просмотр, копирование, создание, обновление, удаление
   - Можно отследить кто и когда получал доступ к секретам

4. **Шифрование** (TODO)
   - Планируется шифрование значений перед сохранением в БД
   - Использование crypto для шифрования/дешифрования

### 📋 Функционал

1. **Добавление секретов**
   - Ключ (например: `DATABASE_URL`)
   - Значение (конфиденциальные данные)
   - Описание (опционально)
   - Уникальность ключа в рамках проекта

2. **Просмотр секретов**
   - По умолчанию значения скрыты (показаны как `••••••`)
   - Кнопка для показа/скрытия значения
   - Логирование каждого просмотра

3. **Копирование в буфер обмена**
   - Быстрое копирование значения секрета
   - Логирование операции копирования

4. **Удаление секретов**
   - Подтверждение перед удалением
   - Каскадное удаление из таблицы логов

## Структура БД

### Таблица `ProjectSecrets`

```sql
CREATE TABLE ProjectSecrets (
    id INT IDENTITY(1,1) PRIMARY KEY,
    project_id INT NOT NULL,
    [key] NVARCHAR(255) NOT NULL,
    value NVARCHAR(MAX) NOT NULL,
    description NVARCHAR(500) NULL,
    created_by INT NOT NULL,
    created_at DATETIME NOT NULL,
    updated_at DATETIME NOT NULL,
    CONSTRAINT UQ_ProjectSecrets_Key UNIQUE (project_id, [key])
);
```

### Таблица `SecretAccessLog`

```sql
CREATE TABLE SecretAccessLog (
    id INT IDENTITY(1,1) PRIMARY KEY,
    secret_id INT NOT NULL,
    user_id INT NOT NULL,
    action NVARCHAR(50) NOT NULL, -- 'view', 'copy', 'create', 'update', 'delete'
    accessed_at DATETIME NOT NULL
);
```

### Поле в таблице `Projects`

```sql
ALTER TABLE Projects 
ADD master_password_hash NVARCHAR(255) NULL;
```

## Установка

1. **Создать таблицы в БД:**
   ```bash
   # Выполнить SQL скрипт
   sql/create_project_secrets.sql
   ```

2. **Установить bcrypt для хеширования паролей (рекомендуется):**
   ```bash
   npm install bcrypt
   npm install --save-dev @types/bcrypt
   ```

## Использование

### В компоненте ProjectView

```tsx
import Secrets from './components/Secrets';

// В табе "Секреты"
{activeTab === 'secrets' && (
  <Secrets projectId={projectId} />
)}
```

### Server Actions

```typescript
import { 
  verifyMasterPassword,
  getProjectSecrets,
  addProjectSecret,
  deleteProjectSecret,
  logSecretAccess
} from './actions/getSecrets';

// Проверка пароля
const isValid = await verifyMasterPassword(projectId, password);

// Получение секретов
const secrets = await getProjectSecrets(projectId);

// Добавление секрета
const secret = await addProjectSecret(projectId, 'API_KEY', 'value', 'description');

// Удаление секрета
await deleteProjectSecret(secretId);

// Логирование
await logSecretAccess(secretId, 'view');
```

## Будущие улучшения

### Приоритет 1
- [ ] Реализовать шифрование значений секретов
- [ ] Использовать bcrypt для хеширования мастер-пароля
- [ ] Добавить UI для установки мастер-пароля проекта

### Приоритет 2
- [ ] Показывать уведомление при успешном копировании
- [ ] Добавить редактирование секретов
- [ ] История изменений секретов
- [ ] Экспорт/импорт секретов (с шифрованием)

### Приоритет 3
- [ ] Роли и права доступа к секретам
- [ ] Временный доступ к секретам (expires)
- [ ] 2FA для доступа к особо важным секретам
- [ ] Аудит лог с детальной информацией

## Безопасность

### ⚠️ Важно

1. **Не храните мастер-пароль в открытом виде**
   - Используйте bcrypt или аналог для хеширования
   - Минимальная стойкость: 10 раундов bcrypt

2. **Не логируйте значения секретов**
   - В логах должны быть только метаданные
   - Никогда не выводите значения в console.log

3. **Используйте HTTPS**
   - Все передачи данных должны быть зашифрованы
   - В production включите TLS для MSSQL

4. **Ограничьте доступ**
   - Проверяйте права пользователя на проект
   - Добавьте роли (только владелец/админ проекта)

## Примеры использования

### Типичные секреты для проекта

```
DATABASE_URL: Server=prod.db.com;Database=MyApp;User=app_user;Password=***;
API_KEY_OPENAI: sk-proj-***
API_KEY_STRIPE: sk_live_***
JWT_SECRET: ***
ENCRYPTION_KEY: ***
AWS_ACCESS_KEY: AKIA***
AWS_SECRET_KEY: ***
SMTP_PASSWORD: ***
```

### Пример добавления через UI

1. Открыть таб "Секреты"
2. Ввести мастер-пароль (минимум 4 символа)
3. Нажать "Разблокировать"
4. Нажать "+ Добавить секрет"
5. Заполнить форму:
   - Ключ: `DATABASE_URL`
   - Значение: `Server=...`
   - Описание: `Production database connection`
6. Нажать "Добавить"

## Troubleshooting

### Ошибка: "Секрет с таким ключом уже существует"
- В рамках одного проекта ключи должны быть уникальными
- Используйте другое имя или удалите существующий секрет

### Мастер-пароль не работает
- Убедитесь, что поле `master_password_hash` создано в таблице Projects
- Проверьте логи сервера на ошибки проверки пароля

### Секреты не загружаются
- Проверьте, что таблица `ProjectSecrets` создана
- Убедитесь, что пользователь авторизован
- Проверьте консоль браузера на ошибки

## Лицензия

Внутренний компонент CRM системы.
