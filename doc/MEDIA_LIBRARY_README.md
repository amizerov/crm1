# Медиатека проекта

## Описание
Медиатека проекта - это система управления изображениями для каждого проекта отдельно. Каждый проект имеет свою собственную папку с изображениями в `public/media/p{projectId}/`.

## Функциональность

### 1. Просмотр медиатеки
- Кнопка "📁" в панели инструментов редактора описания проекта
- Отображает все загруженные изображения проекта
- Предпросмотр выбранного изображения
- Информация о размере файла и имени

### 2. Загрузка изображений
- Кнопка "Загрузить изображение" в медиатеке
- Поддерживаемые форматы: JPEG, JPG, PNG, GIF, WebP
- Максимальный размер файла: 5 МБ
- Автоматическое создание уникальных имен файлов
- Сохранение в папку `public/media/p{projectId}/`

### 3. Вставка изображений
- Выбор изображения в левой панели медиатеки
- Предпросмотр в правой панели
- Кнопка "Вставить выбранное" для добавления в редактор
- Изображение вставляется с возможностью изменения размера

### 4. Удаление изображений
- Кнопка 🗑️ рядом с каждым изображением в списке
- Подтверждение удаления
- Безвозвратное удаление файла из файловой системы

## Структура файлов

### Компоненты
- `MediaLibrary.tsx` - основной компонент медиатеки
- `TiptapEditor.tsx` - интеграция кнопки медиатеки в редактор

### API
- `GET /api/projects/[projectId]/images` - получение списка изображений
- `DELETE /api/projects/[projectId]/images` - удаление изображения

### Действия
- `uploadProjectImage` - загрузка изображений (существующее действие)

## Использование

### Загрузка из файловой системы
1. Нажать кнопку "📷" в панели инструментов редактора
2. Выбрать файл изображения
3. Изображение автоматически загружается и вставляется в редактор

### Загрузка через медиатеку
1. Нажать кнопку "📁" в панели инструментов редактора
2. В открывшейся медиатеке нажать "Загрузить изображение"
3. Выбрать файл изображения
4. Изображение добавляется в медиатеку

### Вставка из медиатеки
1. Нажать кнопку "📁" в панели инструментов редактора
2. Выбрать изображение из списка в левой панели
3. Просмотреть предпросмотр в правой панели
4. Нажать "Вставить выбранное"
5. Изображение вставляется в текущую позицию курсора в редакторе

### Удаление изображений
1. Открыть медиатеку кнопкой "📁"
2. Нажать кнопку 🗑️ рядом с ненужным изображением
3. Подтвердить удаление
4. Изображение удаляется из медиатеки и файловой системы

## Безопасность
- Проверка авторизации пользователя через `getCurrentUser()`
- Валидация типов файлов и размеров
- Проверка принадлежности изображений к конкретному проекту
- Защита от загрузки файлов вне папки проекта

## UI/UX особенности
- Отзывчивый дизайн с темной темой
- Индикаторы загрузки и прогресса
- Подтверждение критических действий (удаление)
- Drag-and-drop интерфейс (планируется)
- Полноэкранный режим медиатеки
- Сортировка по дате загрузки (новые сначала)

## Файловая структура
```
public/
  media/
    p18/                   # ID проекта (p + projectId)
      image1.jpg
      image2.png
      ...
    p25/                   # Другой проект
      photo1.jpg
      ...
```

## Планируемые улучшения
- Поиск по именам файлов
- Группировка по дате загрузки
- Массовое удаление
- Drag-and-drop загрузка
- Редактирование метаданных изображений
- Обрезка и базовое редактирование изображений