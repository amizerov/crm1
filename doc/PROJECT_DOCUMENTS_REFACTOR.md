# Рефакторинг системы документов проекта

## ✅ Реализовано

### Проблема
Документы проекта отображались неправильно - система сканировала файловую систему и показывала документы задач вместе с документами проекта.

### Решение
Переделана архитектура для правильного разделения документов:

## 🏗️ Архитектура

### Таблицы БД
- **ProjectDocuments** - документы проекта (project_id, filename, originalName, filePath, mimeType, fileSize, uploaded_by, uploaded_at)
- **TaskDocuments** - документы задач (через связь с Task.projectId)

### Server Actions

#### `getDocuments.ts`
```typescript
// Получение документов проекта из ProjectDocuments
getProjectDocuments(projectId: number): Promise<ProjectDocument[]>

// Получение документов задач проекта из TaskDocuments
getTaskDocuments(projectId: number): Promise<ProjectDocument[]>
```

#### `uploadDocument.ts`
```typescript
// Загрузка документов проекта в папку public/media/p{projectId}/
uploadProjectDocument(projectId: number, formData: FormData): Promise<UploadDocumentResult>
```

#### `deleteDocument.ts`
```typescript
// Удаление документов с проверкой прав доступа
deleteProjectDocument(documentId: number, source: 'project' | 'task'): Promise<DeleteDocumentResult>
```

## 🎨 UI Компоненты

### Documents.tsx
- **Два режима просмотра**: Документы проекта | Документы задач
- **Загрузка файлов**: Только для документов проекта
- **Отображение источника**: Четкое разделение по типам документов
- **Безопасность**: Удаление только собственных файлов

### ProjectView.tsx  
- **Разделенное состояние**: `projectDocuments` и `taskDocuments`
- **Подсчет**: Общее количество всех документов в счетчике вкладки
- **Правильная загрузка данных**: Параллельные запросы к обеим таблицам

## 📁 Файловая структура

```
public/media/
  p{projectId}/              # Документы проекта
    ├── timestamp_filename   # Загруженные файлы проекта
  p{projectId}/
    └── t{taskId}/          # Документы задач (как было)
        ├── timestamp_file1
        └── timestamp_file2
```

## 🔒 Безопасность

1. **Проверка авторизации** - только авторизованные пользователи
2. **Проверка прав на удаление** - только загрузивший файл может его удалить  
3. **Валидация размера** - максимум 50 МБ
4. **Безопасные пути** - проверка корректности путей к файлам
5. **Уникальные имена** - timestamp предотвращает конфликты имен

## 🎯 Результат

### ✅ Исправлено
- Документы проекта больше НЕ включают документы из задач
- Четкое разделение по источникам (проект/задача)
- Правильная работа с таблицами БД вместо сканирования ФС
- Безопасность загрузки и удаления файлов

### ✅ Добавлено
- Переключатель режимов просмотра
- Подсчет документов по типам
- Загрузка документов напрямую в проект
- Индикация источника документа (проект/задача)

### ✅ Сохранено
- Вся существующая функциональность документов задач
- Совместимость со старыми путями файлов
- UI/UX консистентность

## 🧪 Тестирование

Убедиться что:
1. Документы проекта отображаются только в соответствующей вкладке
2. Документы задач отображаются отдельно с указанием задачи
3. Загрузка файлов работает в директорию `public/media/p{projectId}/`
4. Удаление работает только для собственных файлов
5. Счетчики вкладок корректно отображают количество документов

## 📚 Связанные файлы

- `src/app/tasks/views/project/components/Documents.tsx`
- `src/app/tasks/views/project/actions/getDocuments.ts`
- `src/app/tasks/views/project/actions/uploadDocument.ts`  
- `src/app/tasks/views/project/actions/deleteDocument.ts`
- `src/app/tasks/views/project/ProjectView.tsx`
- SQL: `CREATE TABLE ProjectDocuments` (уже создана)